# Builder Construction Studio

Standalone Sanity Studio for project `69j8m4rs`, dataset `production`.

Requires Node.js 20.19.1 or newer.

```bash
npm install
npm run dev
```

The Studio runs at [http://localhost:3333](http://localhost:3333). Run the
Next.js app separately from `../web`.
